query APP_INIT {
  config @client {
    googleTagManager {
      id
    }
    i18n {
      lng
    }
  }
}

query GET_BACKEND_CONFIG {
  backendConfig {
    locales
    activeLocale
    shop {
      activeCurrency
    }
  }
}

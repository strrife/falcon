---
name: Radio
menu: Components
---

import { Playground } from 'docz';
import { ThemePreview, themeWithCustomRadioCheck } from 'docs';
import { Radio, ThemeProvider } from '@deity/falcon-ui';

<ThemePreview of={Radio} />

`Radio` component size can be easily adjusted via theme-based `size` prop,
 but it also accepts other [responsive, theme-based style props](/responsive-props).
 
```javascript
import { Radio } from '@deity/falcon-ui';
```

<Playground>
  <Radio name="size" size="sm" mr="md" />
  <Radio name="size" mr="md" defaultChecked />
  <Radio name="size" size="lg" />
</Playground>

Custom `Radio` checked icon can be provided via icons section of `Theme`.

<Playground>
  <ThemeProvider theme={themeWithCustomRadioCheck}>
    <Radio name="custom" size="sm" mr="md" />
    <Radio name="custom" mr="md" defaultChecked />
    <Radio name="custom" size="lg" />
  </ThemeProvider>
</Playground>

```javascript
const themeWithCustomRadioCheck = createTheme({
  icons: {
    radioCheckedIcon: {
      icon: Circle
    }
  }
});
```

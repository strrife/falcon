---
name: Checkbox
menu: Components
---

import { Playground } from 'docz';
import { ThemePreview, themeWithCustomCheckmark } from 'docs';
import { Checkbox, ThemeProvider } from '@deity/falcon-ui';

<ThemePreview of={Checkbox} />

`Checkbox` component size can be easily adjusted via theme-based `size` prop,
 but it also accepts other [responsive, theme-based style props](/responsive-props).
 
```javascript
import { Checkbox } from '@deity/falcon-ui';
```

<Playground>
  <Checkbox size="sm" mr="md" />
  <Checkbox mr="md" defaultChecked />
  <Checkbox size="lg" />
</Playground>

Custom `Checkbox` checkmark icon can be provided via icons section of `Theme`.

<Playground>
  <ThemeProvider theme={themeWithCustomCheckmark}>
    <Checkbox size="sm" mr="md" />
    <Checkbox mr="md" defaultChecked />
    <Checkbox size="lg" />
  </ThemeProvider>
</Playground>

```javascript
const themeWithCustomCheckmark = createTheme({
  icons: {
    checkboxCheckmarkIcon: {
      icon: CustomIconSvgComponent,
      fill: 'white'
    }
  }
});
```

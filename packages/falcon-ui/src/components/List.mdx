---
name: List
menu: Components
---

import { Playground } from 'docz';
import { ThemePreview } from 'docs';
import { List, ListItem } from '@deity/falcon-ui';

<ThemePreview of={[List, ListItem]} />

`List` and `ListItem` components by default render as `<ul>` and `<li>` tags and accept all of the [responsive, theme-based style props](/responsive-props).

```javascript
import { List, ListItem } from '@deity/falcon-ui';
```

<Playground>
  <List>
    <ListItem>Item</ListItem>
    <ListItem>Another item</ListItem>
    <ListItem>Last</ListItem>
  </List>
</Playground>


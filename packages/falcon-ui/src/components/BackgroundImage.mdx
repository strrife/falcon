---
name: BackgroundImage
menu: Components
---

import { Playground } from 'docz';
import { ThemePreview, CustomPropsTable } from 'docs';
import { BackgroundImage } from '@deity/falcon-ui';

<ThemePreview of={BackgroundImage} />

`BackgroundImage` component allows setting background image as well as its aspect ratio via `src` and `ratio` props.

```javascript
import { BackgroundImage } from '@deity/falcon-ui';
```

<Playground>
  <BackgroundImage
    ratio={0.5}
    src="https://images.unsplash.com/photo-1533359856343-b66cefc8bdd7?ixlib=rb-0.3.5&s=e6f5d8aca61ab3651463fc562d7e61f4&auto=format&fit=crop&w=1500&q=80"
  />
</Playground>

<CustomPropsTable componentName="Backdrop" props={[{
  name: 'src',
  type: 'string',
  required: true,
  defaultValue: '',
  description: 'image source that will be displayed as a background'
  }, {
  name: 'ratio',
  type: 'number',
  required: true,
  defaultValue: 1,
  description: 'aspect ratio of the image the background'
  }]}
 />

`BackgroundImage` component accepts also all of the [responsive, theme-based style props](/responsive-props).
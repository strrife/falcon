---
name: Input
menu: Components
---

import { Playground } from 'docz';
import { ThemePreview, CustomPropsTable } from 'docs';
import { Input, Box, Label } from '@deity/falcon-ui';

<ThemePreview of={Input} />

`Input` component is rendered as an `<input type="text">` and accepts all of the `<input>` props (`type`, `onChange` etc).


```javascript
import { Input } from '@deity/falcon-ui';
```


<Playground>
  <Box>
    <Input placeholder="Hello!" />
    <Input mt="lg" type="password" placeholder="Password" />
    <Input mt="lg" type="email" placeholder="Email" invalid />
    <Label mt="lg">As textarea:</Label>
    <Input as="textarea" placeholder="Input as Textarea!" />
  </Box>
</Playground>

<CustomPropsTable componentName="Input" props={[{
  name: 'type',
  type: 'string',
  required: false,
  defaultValue: '"text"',
  description: 'type of the input'
  }, {
  name: 'invalid',
  type: 'boolean',
  required: false,
  defaultValue: 'false',
  description: 'when set to <code>true</code> input provide visual indication (by default border color) that content provided to the input is invalid'
  }]}
 />

 `Input` component accepts also all of the [responsive, theme-based style props](/responsive-props).
